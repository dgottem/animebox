window.onload = function () {
    // Checks if the screen width is 2500px or wider
    if (window.innerWidth >= 2500) {
        alert("Due to the width of your screen, zoom in for a better experience.");
    }
};